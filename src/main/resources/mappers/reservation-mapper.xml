<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="reservation">

	<select id="selectList" resultType="ReservationRespDto">
		SELECT * FROM RESERVATION
	</select>
	
	<select id="resListOne" resultType="ReservationRespDto" >
		select * 
		 from reservation
		 join member on reservation.idx = member.idx
		 	 where username = #{username}
	</select>
	
	<update id="resUpdate">
		UPDATE RESERVATION SET RESERV_START=#{reserv_start}, RESERV_END=#{reserv_end},
							RESERV_STATE=#{reserv_state},INSURANCE_IDX=#{insurance_idx},
							VEHICLE_IDX=#{vehicle_idx},RESERV_START=#{RENT_LOCATION},
							RESERV_START=#{RETURN_LOCATION},RESERV_START=#{COUPON_IDX},
				  WHERE IDX = #{IDX}
	</update>
	
	<delete id="resDelete">
		DELETE FROM RESERVATION WHERE IDX = #{IDX}
	</delete>
</mapper>
