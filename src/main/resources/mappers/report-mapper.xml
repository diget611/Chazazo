<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="report">

	<select id="selectList" resultType="adminReportRespDto">
		SELECT REPORT.*, USERNAME FROM REPORT INNER JOIN MEMBER ON REPORT.MEMBER_IDX = MEMBER.IDX ORDER BY REPORT.IDX DESC
	</select>
	
	<select id="selectOne" resultType="adminReportRespDto">
		SELECT REPORT.*, USERNAME FROM REPORT INNER JOIN MEMBER ON REPORT.MEMBER_IDX = MEMBER.IDX WHERE REPORT.IDX = #{idx}
	</select>
	
</mapper>
